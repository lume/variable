import {variable, autorun, reactive} from './index'

describe('variable', () => {
	it('needs testing', () => {
		expect(variable).toBeInstanceOf(Function)
		expect(autorun).toBeInstanceOf(Function)
		expect(reactive).toBeInstanceOf(Function)
	})
})
